package finalProject;

public interface CourseInterface {
	
	// Initializing the Framework for the addCourse and removeCourse functions
    void addCourse(Course course);
    void removeCourse(Course course);
}